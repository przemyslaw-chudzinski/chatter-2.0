import { TabNavBarDirective } from './tab-nav-bar.directive';

describe('TabNavBarDirective', () => {
  it('should create an instance', () => {
    const directive = new TabNavBarDirective();
    expect(directive).toBeTruthy();
  });
});

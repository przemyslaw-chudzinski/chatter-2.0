import { StretchTabsDirective } from './stretch-tabs.directive';

describe('StretchTabsDirective', () => {
  it('should create an instance', () => {
    const directive = new StretchTabsDirective();
    expect(directive).toBeTruthy();
  });
});

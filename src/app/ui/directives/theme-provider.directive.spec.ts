import { ThemeProviderDirective } from './theme-provider.directive';

describe('ThemeProviderDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeProviderDirective();
    expect(directive).toBeTruthy();
  });
});
